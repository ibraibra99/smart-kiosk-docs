6.4 — البثّ الحقيقيّ (streaming)
🔴 يحتاج **إعادة بناء مسار الردّ** لا تعديلَ واجهة:
   • `chat.dart` يُرجع JSON مكتملاً — لا قناةً مفتوحة
   • والنصّ **يُنظَّف بعد اكتماله** (cleanTextForScreen يحذف صيغة الصورة)
     ⇒ بثُّ الخام يُظهر `

![...](...)

` للزبون
   • والأزرار والمنتجات تُستخرَج من نفس النصّ
⏰ المحفِّز: أن يثبت أنّ 6.2 لا تكفي — لا قبله.